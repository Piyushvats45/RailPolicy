# IRCTC Policy Assistant — RAG Q&A with a React Frontend

A retrieval-augmented generation (RAG) system that answers questions about
IRCTC's train ticket cancellation and refund policy, grounded in a real
FAQ knowledge base, with cited sources instead of hallucinated answers.
Full-stack: a FastAPI backend serving the RAG pipeline, and a React
frontend for a real chat UI. The LLM runs locally via Ollama.

## Why this problem

Policy documents (refund rules, eligibility criteria, procedures) are
exactly where LLMs confidently hallucinate incorrect details if asked
from memory alone. RAG fixes that: answers are traceable back to a real
source chunk, and the model is instructed to say "I don't know" rather
than guess.

## Live URL
https://rail-policy.vercel.app

## Architecture

```
React frontend (Vite)
     │  POST /api/ask { question }
     ▼
FastAPI backend
     │
     ▼
Embed question (sentence-transformers, all-MiniLM-L6-v2)
     │
     ▼
Search FAISS vector index for top-k nearest chunks
     │
     ▼
Pass question + retrieved chunks to a local LLM (Llama 3.2 via Ollama),
instructed to answer ONLY from those chunks and cite the source number
     │
     ▼
Grounded, cited answer -> returned as JSON -> rendered in the chat UI
```

## Project structure

```
RailPolicy/
├── backend/
│   ├── data/
│   │   ├── scrape_faqs.py      # generic scraper template
│   │   ├── faqs_raw.json       # sample FAQ dataset
│   │   ├── chunks.json         # generated by chunking.py
│   │   ├── faq_index.faiss     # generated by build_index.py
│   │   └── faq_meta.pkl        # generated by build_index.py
│   ├── src/
│   │   ├── chunking.py         # FAQ-pair chunking + general sliding-window chunker
│   │   ├── build_index.py      # embed chunks, build FAISS index
│   │   └── rag_pipeline.py     # retrieval + grounded generation (Ollama)
│   ├── main.py                 # FastAPI server (/api/ask, /api/health)
│   ├── evaluate.py             # Hit Rate @ k retrieval evaluation
│   └── requirements.txt
└── frontend/
    ├── index.html
    ├── package.json
    ├── vite.config.js
    └── src/
        ├── main.jsx
        ├── App.jsx             # header, conversation log, input
        ├── App.css             # design tokens + styling
        ├── index.css           # global resets, focus states
        └── components/
            ├── ChatMessage.jsx # message bubble + ticket-stub source cards
            └── ChatInput.jsx
```

